apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-app
spec:
  selector:
    matchLabels:
      app: dev-app
  replicas: 1
  template:
    metadata:
      labels:
        app: dev-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: environment
                operator: In
                values:
                - development
      containers:
        - image: nginx:latest
          name: nginx
